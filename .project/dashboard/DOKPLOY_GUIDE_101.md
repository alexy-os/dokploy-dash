# Dokploy 101 - Полный гайд для новичка

## 📋 Содержание
1. [Структура и расположение файлов](#структура-и-расположение-файлов)
2. [Основные команды Bash](#основные-команды-bash)
3. [Интерфейс Dokploy](#интерфейс-dokploy)
4. [Резервные копии и восстановление](#резервные-копии-и-восстановление)
5. [Миграция на другой хост](#миграция-на-другой-хост)
6. [Мониторинг и логи](#мониторинг-и-логи)
7. [Лучшие практики](#лучшие-практики)
8. [Решение проблем](#решение-проблем)

---

## Структура и расположение файлов

### 📁 Главные директории Dokploy

```
Ваш сервер:
├── /etc/dokploy/                          # ⭐ Основная конфигурация
│   ├── traefik/                           # Конфигурация reverse proxy
│   │   ├── traefik.yml                    # Основные настройки
│   │   └── dynamic/                       # Динамические правила (SSL, домены)
│   └── [другие конфиги]
│
├── /var/lib/docker/volumes/               # 💾 Данные контейнеров
│   ├── dokploy-postgres-database/         # База данных Dokploy
│   ├── redis-data-volume/                 # Кэш Redis
│   ├── dokploy-docker-config/             # Docker credentials
│   └── [volumes проектов]
│
└── /opt/dokploy-projects/                 # 📦 Ваши проекты (по умолчанию)
    └── [проект1, проект2, ...]
```

### 🔍 Что где расположено?

| Назначение | Путь | Важность |
|-----------|------|---------|
| Конфигурация Traefik | `/etc/dokploy/traefik/` | 🔴 КРИТИЧНО |
| База данных Dokploy | `/var/lib/docker/volumes/dokploy-postgres-database/` | 🔴 КРИТИЧНО |
| Данные Redis | `/var/lib/docker/volumes/redis-data-volume/` | 🟡 Важно |
| Docker конфиги | `/var/lib/docker/volumes/dokploy-docker-config/` | 🟡 Важно |
| Проекты пользователей | `/opt/dokploy-projects/` | 🟡 Важно |
| SSL сертификаты | `/etc/dokploy/traefik/dynamic/` | 🔴 КРИТИЧНО |

---

## Основные команды Bash

### 🐋 Docker команды для Dokploy

```bash
# ========== СТАТУС И ИНФОРМАЦИЯ ==========

# Посмотреть все сервисы Dokploy
docker service ls

# Проверить статус конкретного сервиса
docker service ps dokploy
docker service ps dokploy-postgres
docker service ps dokploy-redis

# Посмотреть логи сервиса (в реальном времени)
docker service logs dokploy -f           # -f = follow (live)
docker service logs dokploy-postgres -f
docker service logs dokploy-redis -f

# Проверить объем памяти и CPU
docker stats

# ========== ИНФОРМАЦИЯ О VOLUMES ==========

# Посмотреть все volumes
docker volume ls | grep dokploy

# Получить детальную информацию о volume
docker volume inspect dokploy-postgres-database

# Размер volume (приблизительно)
du -sh /var/lib/docker/volumes/dokploy-postgres-database/_data

# ========== ПЕРЕЗАГРУЗКА И ОБНОВЛЕНИЕ ==========

# Перезагрузить сервис Dokploy
docker service update --force dokploy

# Обновить образ Dokploy
docker pull dokploy/dokploy:latest
docker service update --image dokploy/dokploy:latest dokploy

# ========== СЕТЬ ==========

# Посмотреть сети Dokploy
docker network ls | grep dokploy

# Проверить, какие контейнеры в сети
docker network inspect dokploy-network

# ========== РЕЗЕРВНОЕ КОПИРОВАНИЕ ==========

# Экспортировать volume (создать бэкап)
docker run --rm -v dokploy-postgres-database:/data \
  -v $(pwd):/backup busybox tar czf /backup/postgres-backup.tar.gz -C /data .

# Восстановить volume из бэкапа
docker run --rm -v dokploy-postgres-database:/data \
  -v $(pwd):/backup busybox tar xzf /backup/postgres-backup.tar.gz -C /data
```

### 📂 Файловая система - навигация и просмотр

```bash
# Перемещение
cd /etc/dokploy              # Перейти в директорию
pwd                          # Показать текущую директорию
ls -la                       # Список файлов с деталями
tree /etc/dokploy            # Древовидный вид (если установлен)

# Просмотр файлов
cat /etc/dokploy/traefik/traefik.yml           # Показать содержимое файла
head -20 файл.txt                              # Первые 20 строк
tail -50 файл.txt                              # Последние 50 строк
tail -f файл.txt                               # Следить за файлом (live)

# Поиск файлов
find /etc/dokploy -name "*.yml"               # Найти все .yml файлы
grep "server" /etc/dokploy/traefik/traefik.yml # Найти строки с "server"

# Размер
du -sh /etc/dokploy                           # Размер всей папки
du -sh /etc/dokploy/*                         # Размер каждого элемента
```

### 🔐 Права доступа и разрешения

```bash
# Посмотреть права доступа
ls -la /etc/dokploy

# Изменить права (rwx = read, write, execute)
chmod 755 файл              # rwx r-x r-x (стандарт)
chmod 777 файл              # rwx rwx rwx (полный доступ - для Dokploy)
chmod 644 файл              # rw- r-- r-- (для читаемых файлов)

# Изменить владельца
chown root:docker /etc/dokploy

# Применить рекурсивно ко всем файлам
chmod -R 755 /etc/dokploy
```

---

## Интерфейс Dokploy

### 🎯 Что вы найдете в веб-интерфейсе (http://ваш-ip:3000)

#### 1. **Dashboard** - Главная страница
- Статус всех сервисов
- Использование ресурсов (CPU, Memory, Disk)
- Последние события
- Быстрые ссылки

**Что смотреть**: Зеленые галочки = все работает. Красные = проблемы.

#### 2. **Projects** - Управление проектами
- Создание новых проектов
- Развертывание приложений
- Управление окружением (dev, staging, prod)

**Практика**: 
- Создайте тестовый проект с простым приложением
- Научитесь деплоить Docker образы
- Поэкспериментируйте с переменными окружения

#### 3. **Applications** - Приложения и контейнеры
- Список всех запущенных приложений
- Логи каждого приложения
- Перезагрузка, масштабирование
- Переменные окружения

**Важное**: 
- Каждое приложение может иметь несколько реплик (экземпляров)
- Логи хранятся в памяти, не забывайте их смотреть
- Нельзя удалить приложение если оно используется другими сервисами

#### 4. **Databases** - Базы данных
- PostgreSQL, MySQL, MongoDB и др.
- Создание резервных копий
- Восстановление из бэкапов
- Экспорт данных

**Советы**:
- Создавайте ежедневные бэкапы для баз
- Используйте разные базы для разных проектов
- Называйте базы понятно: `db_project_name_prod`

#### 5. **Settings** - Настройки
- Информация о сервере
- Интеграции (GitHub, GitLab, S3 - вы уже настроили!)
- Пользователи и безопасность
- Резервные копии
- SSL сертификаты (вы уже настроили!)

**Проверить**:
- ✅ S3 интеграция (для бэкапов)
- ✅ SSL сертификаты (Traefik/Let's Encrypt)
- ✅ Домены и DNS
- ✅ Уведомления (если нужны)

#### 6. **Cluster** - Информация о Docker Swarm
- Узлы в кластере
- Статус Swarm
- Работники и менеджеры

**Понимание**: 
- У вас 1 узел (Manager) = однонодовой кластер
- В будущем можете добавить worker узлы
- Для управления 5+ серверами используются Manager узлы

#### 7. **Monitoring** - Мониторинг (если включен)
- Графики использования ресурсов
- История логов
- Оповещения

---

## Резервные копии и восстановление

### 🔄 Полная стратегия резервного копирования

#### ✅ ЧТО нужно бэкапить

```
КРИТИЧНО (ежедневно):
├── /etc/dokploy/                    # Конфигурация
├── /var/lib/docker/volumes/dokploy-postgres-database/  # БД
└── /var/lib/docker/volumes/redis-data-volume/          # Redis

ВАЖНО (2 раза в неделю):
└── /opt/dokploy-projects/           # Ваши проекты
```

### 📦 Способ 1: Автоматические бэкапы через Dokploy UI

```
Dokploy Settings → Backups:
1. Нажмите "Create Backup"
2. Выберите S3 (уже настроено!)
3. Включите автоматические бэкапы
4. Установите расписание: ежедневно в 3:00 AM
```

### 💾 Способ 2: Ручное бэкапирование через команды

```bash
# Бэкап PostgreSQL базы
docker run --rm -v dokploy-postgres-database:/data \
  -v $(pwd):/backup ubuntu tar czf /backup/dokploy-db-$(date +%Y%m%d).tar.gz -C /data .

# Бэкап конфигурации Dokploy
tar -czf /backup/dokploy-config-$(date +%Y%m%d).tar.gz /etc/dokploy

# Бэкап всех volumes одной командой
for vol in dokploy-postgres-database redis-data-volume dokploy-docker-config; do
  docker run --rm -v $vol:/data -v $(pwd):/backup ubuntu \
    tar czf /backup/${vol}-$(date +%Y%m%d).tar.gz -C /data .
done

# Отправить бэкап на S3
aws s3 cp /backup/ s3://ваш-bucket/dokploy-backups/ --recursive
```

### 🔄 Восстановление из бэкапа

```bash
# 1. Остановить сервисы
docker service rm dokploy dokploy-postgres dokploy-redis

# 2. Удалить старые volumes
docker volume rm dokploy-postgres-database redis-data-volume

# 3. Создать новые volumes
docker volume create dokploy-postgres-database
docker volume create redis-data-volume

# 4. Восстановить данные
docker run --rm -v dokploy-postgres-database:/data \
  -v /backup:/backup ubuntu tar xzf /backup/dokploy-db-ДАТА.tar.gz -C /data

# 5. Перезапустить скрипт установки
/root/install_dokploy.sh
```

---

## Миграция на другой хост

### 🚚 Полный перенос на новый сервер

#### Подготовка на СТАРОМ сервере:

```bash
# 1. Создать полный бэкап
mkdir -p /backup/dokploy-migration
cd /backup/dokploy-migration

# 2. Бэкап всех данных
for vol in dokploy-postgres-database redis-data-volume dokploy-docker-config; do
  docker run --rm -v $vol:/data -v $(pwd):/backup ubuntu \
    tar czf /backup/dokploy-migration/${vol}.tar.gz -C /data .
done

# 3. Бэкап конфигурации
tar -czf /backup/dokploy-migration/config.tar.gz /etc/dokploy

# 4. Бэкап проектов (если они локально)
tar -czf /backup/dokploy-migration/projects.tar.gz /opt/dokploy-projects

# 5. Скопировать на новый сервер через SCP
scp -r /backup/dokploy-migration root@новый-сервер:/root/
```

#### На НОВОМ сервере:

```bash
# 1. Установить Dokploy
/root/install_dokploy.sh

# 2. Остановить новые пустые сервисы
docker service rm dokploy dokploy-postgres dokploy-redis

# 3. Удалить volumes
docker volume rm dokploy-postgres-database redis-data-volume dokploy-docker-config

# 4. Восстановить из бэкапа
cd /root/dokploy-migration

# Восстановить volumes
for vol in dokploy-postgres-database redis-data-volume dokploy-docker-config; do
  docker volume create $vol
  docker run --rm -v $vol:/data -v $(pwd):/backup ubuntu \
    tar xzf /backup/dokploy-migration/${vol}.tar.gz -C /data .
done

# Восстановить конфигурацию
tar -xzf /backup/dokploy-migration/config.tar.gz -C /

# 5. Перезапустить сервисы
docker service create \
  --name dokploy-postgres \
  --constraint 'node.role==manager' \
  --network dokploy-network \
  --env POSTGRES_USER=dokploy \
  --env POSTGRES_DB=dokploy \
  --env POSTGRES_PASSWORD=amukds4wi9001583845717ad2 \
  --mount type=volume,source=dokploy-postgres-database,target=/var/lib/postgresql/data \
  postgres:16

# 6. Запустить Dokploy
docker pull dokploy/dokploy:latest
docker service create \
  --name dokploy \
  --network dokploy-network \
  --mount type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock \
  --mount type=volume,source=dokploy-docker-config,target=/root/.docker \
  --publish published=3000,target=3000,mode=host \
  --constraint 'node.role == manager' \
  dokploy/dokploy:latest

# 7. Проверить статус
docker service ls
docker service logs dokploy -f
```

---

## Мониторинг и логи

### 📊 Основные метрики для отслеживания

```bash
# Мониторинг в реальном времени
watch -n 1 'docker stats'           # Обновляется каждую секунду

# Использование диска
df -h                               # Общее место
du -sh /var/lib/docker/volumes/*    # Размер каждого volume

# Использование памяти
free -h

# Использование CPU
top -b -n 1 | head -20

# Сетевая активность
nethogs                             # Если установлено
```

### 📝 Просмотр логов

```bash
# Логи конкретного сервиса (последние 100 строк, в реальном времени)
docker service logs dokploy -f --tail 100

# Логи за определенный период
docker service logs dokploy --since 1h     # За последний час
docker service logs dokploy --until 30m    # До 30 минут назад

# Фильтрация логов по ошибкам
docker service logs dokploy 2>&1 | grep -i error

# Сохранить логи в файл
docker service logs dokploy > /tmp/dokploy-logs.txt 2>&1

# Просмотр логов Traefik
docker logs dokploy-traefik -f --tail 100

# Системные логи сервера
journalctl -xe --no-pager          # Последние события
journalctl -u docker -n 50          # Логи Docker daemon
```

### 🚨 Что смотреть в логах

```
❌ ОШИБКИ (нужно исправлять):
- "connection refused"      - Сервис не запустился
- "out of memory"          - Недостаточно памяти
- "permission denied"      - Проблема с правами доступа

⚠️  ПРЕДУПРЕЖДЕНИЯ (обращать внимание):
- "pull rate limit"        - Исчерпан лимит Docker Hub
- "health check failed"    - Сервис не здоров
- "high disk usage"        - Мало место на диске
```

---

## Лучшие практики

### ✅ Безопасность

```bash
# 1. Регулярно обновляйте систему
apt update && apt upgrade -y

# 2. Используйте firewall
ufw enable
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw allow 3000/tcp  # Dokploy (закройте в продакшене!)

# 3. Ограничьте доступ к Dokploy через nginx/reverse proxy
# (не открывайте 3000 порт в интернет)

# 4. Используйте SSH ключи, не пароли
ssh-keygen -t ed25519 -C "server-key"

# 5. Включите 2FA в Dokploy settings
```

### 📈 Производительность

```
1. Регулярно чистите неиспользуемые images:
   docker image prune -a --force

2. Ограничивайте ресурсы для каждого контейнера в Dokploy UI

3. Используйте отдельные базы для каждого проекта

4. Регулярно архивируйте старые логи

5. Следите за свободным местом на диске (держите 20% свободным)
```

### 📋 Планирование

```
ЕЖЕДНЕВНО:
- Проверить Dashboard в Dokploy
- Просмотреть логи на ошибки
- Убедиться, что бэкапы создаются

ЕЖЕНЕДЕЛЬНО:
- Проверить использование ресурсов (CPU, RAM, Disk)
- Обновить Docker образы
- Проверить SSL сертификаты (должны быть действительными)

ЕЖЕМЕСЯЧНО:
- Обновить ОС сервера
- Провести тест восстановления из бэкапа
- Очистить неиспользуемые Docker объекты
- Проверить логи на причины сбоев
```

---

## Решение проблем

### 🔧 Частые проблемы и их решение

#### Проблема 1: "Service is stuck in preparing"

```bash
# Решение:
# 1. Проверить логи
docker service logs dokploy-postgres -f

# 2. Может быть, не хватает места
df -h

# 3. Может быть, образ не загружается
docker pull postgres:16

# 4. Если ничего не помогает, переинициализировать:
docker service rm dokploy-postgres
docker volume rm dokploy-postgres-database
docker volume create dokploy-postgres-database
# Переустановить скрипт установки
```

#### Проблема 2: "Cannot connect to Dokploy dashboard"

```bash
# Проверить, запущен ли сервис
docker service ps dokploy

# Проверить логи
docker service logs dokploy -f --tail 50

# Перезагрузить сервис
docker service update --force dokploy

# Проверить, свободен ли порт 3000
ss -tulnp | grep 3000
```

#### Проблема 3: "SSL certificate error"

```bash
# Проверить сертификаты Traefik
docker logs dokploy-traefik -f --tail 50

# Проверить конфигурацию
cat /etc/dokploy/traefik/traefik.yml

# Перезапустить Traefik
docker restart dokploy-traefik

# Обновить сертификаты Let's Encrypt
docker exec dokploy-traefik traefik --version
```

#### Проблема 4: "Disk space full"

```bash
# Посмотреть, что занимает место
du -sh /var/lib/docker/volumes/*
du -sh /var/lib/docker/containers/*

# Очистить неиспользуемые образы
docker image prune -a --force

# Очистить неиспользуемые volumes
docker volume prune --force

# Очистить логи контейнеров
docker system prune --volumes --force
```

#### Проблема 5: "Memory/CPU high usage"

```bash
# Найти, что потребляет ресурсы
docker stats

# Ограничить ресурсы для сервиса (в Dokploy UI):
# Settings → Service → Resource Limits

# Или через команду:
docker service update \
  --limit-cpu 1 \
  --limit-memory 512M \
  имя-сервиса
```

---

## 📚 Дополнительные ресурсы

### Команды которые нужно запомнить

```bash
# Статус всего
docker system df              # Использование ресурсов

# Помощь
docker service help
docker volume help
docker network help

# Расширенная информация
docker service inspect dokploy         # Все детали сервиса
docker volume inspect volume-name      # Все детали volume
```

### Где учиться

- [Docker Documentation](https://docs.docker.com/)
- [Dokploy Documentation](https://dokploy.com/docs)
- [Docker Swarm Guide](https://docs.docker.com/engine/swarm/)
- Ваш лог сервера (журнал ошибок)

---

## 🎯 Ваше действие: Первые шаги

```
1. ✅ Зайдите в Dokploy UI (http://ваш-ip:3000)
2. ✅ Проверьте Dashboard - все ли зелено?
3. ✅ Перейдите в Settings - убедитесь что S3 и SSL работают
4. ✅ Создайте тестовый проект
5. ✅ Разверните простое приложение (например, nginx)
6. ✅ Посмотрите его логи
7. ✅ Создайте ручной бэкап
8. ✅ Изучите файлы в /etc/dokploy/traefik/
```

---

## ❓ Какой вопрос задать сначала?

**"Какой у меня есть объем памяти и CPU?"**

```bash
cat /proc/cpuinfo | grep processor | wc -l   # Количество CPU
free -h                                        # Объем памяти
df -h                                         # Место на диске
```

Это определит, какие приложения вы можете запускать.

---

**Помните**: Начните с простого. Не спешите настраивать сложные конфигурации. Сначала научитесь работать с базовыми операциями, потом переходите на advanced уровень.

